export interface Iproperty {
  data: {
    movId: number;
    person?: string;
    level?: string;
    title: string;
    description: string;
    embedId?: string;
    imageUrl?: string;
  };
}
